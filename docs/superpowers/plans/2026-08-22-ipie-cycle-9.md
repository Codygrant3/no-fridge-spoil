# IPIE Cycle 9 Implementation Plan

> Branch from `main` (`0c8fbf4`). Do not edit files on draft PRs #3–#23.

**Goal:** Household usage “today” uses the local calendar.

## Task: Usage local today

**Branch:** `cursor/usage-local-today-3203`

**Files:**
- Modify: `src/services/usageService.ts`
- Create: `src/__tests__/services/usageService.test.ts`

**Behavior:**
- Compare `usage_day` to `formatDate(new Date())`, not `toISOString().slice(0, 10)`.
- Keep the existing 29-day UTC window query unless a test proves it is wrong; do not redesign the Supabase query.
- Mock `supabaseClient`. No secrets in `VITE_*`.

**Tests:**
- A row whose `usage_day` is today’s `formatDate` increments `todaySuccessful` / `todayDenied`.
- A row on a different calendar day does not.
- `getUsageSummary` throws when `supabase` is null.

**Verify:**
```bash
npx vitest run src/__tests__/services/usageService.test.ts
npm run typecheck
npm run lint
```

Push the branch. Do not open a PR.
