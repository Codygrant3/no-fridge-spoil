# IPIE Cycle 1 Implementation Plan

> **For agentic workers:** Use cloud agents for Tasks 2 and 3. The coordinator owns Task 1. Do not edit files listed on open draft PRs #3, #4, or #5.

**Goal:** Encode the inspect-plan-implement-evaluate loop and fix two disjoint product/test gaps that Cycle 0 left untouched.

**Architecture:** Coordinator writes the loop rule and ledger. One cloud agent owns shopping-list categorization. One cloud agent owns Meal Planner characterization tests. No shared files.

**Tech Stack:** React 19, TypeScript, Dexie, Vitest, Testing Library, existing `useModalFocus` / `localMutationFields` patterns.

## Global Constraints

- Branch from `main` (`0c8fbf4` lineage). Do not stack on PRs #3–#5.
- Never reset or delete untracked files.
- No secrets in `VITE_*`.
- Keep routes lazy. Do not touch `Scan.tsx`, `ReviewItems.tsx`, `Profile.tsx`, `index.css`, receipt OCR/resolver/recovery files, or `api/*`.
- Focused vitest + `npm run typecheck` + `npm run lint`. Do not claim skipped release gates passed.
- Open a draft PR; do not merge.

---

### Task 1: Loop rule and handover refresh (coordinator)

**Files:**
- Create: `.cursor/rules/ipie-loop.mdc`
- Create: `docs/IPIE_LOOP.md`
- Create: `docs/superpowers/plans/2026-08-21-ipie-cycle-1.md`
- Modify: `docs/CURSOR_HANDOVER.md` (status / §18)
- Modify: `.cursor/rules/project-handover.mdc` (dirty-tree sentence is stale)

**Done when:** Draft PR exists for branch `cursor/ipie-loop-3203`.

---

### Task 2: Shopping list category + add-field name

**Files:**
- Modify: `src/services/shoppingActionService.ts` (export `shoppingCategory` if it is not already exported)
- Modify: `src/pages/ShoppingList.tsx`
- Create: `src/__tests__/pages/ShoppingList.test.tsx`
- Test helpers: follow `src/__tests__/pages/Inventory.test.tsx` and `src/__tests__/pages/Alerts.test.tsx` (fake IndexedDB is already in setup)

**Do not edit:** `App.tsx`, receipt services, resolver, Scan, ReviewItems, Profile, CSS.

**Behavior:**
- Remove `const selectedCategory = 'other' as const`.
- When adding a typed item or a Running Low chip, set `category: shoppingCategory(name)`.
- Duplicate-name quantity bump keeps the existing category.
- Add field: accessible name `Item name` (visible label or `aria-label` on the input). Keep the current editorial layout.
- `shoppingCategory('Eggs')` → `dairy`, `'Oat milk'` → `dairy`, `'Coffee beans'` → `beverages`, `'Bread'` → `pantry`, `'Butter'` → `dairy`. Extend the helper only if a Running Low chip would still map to `other`.

**Tests (write first):**
- Adding "Milk" creates a shopping row in the Dairy group.
- Adding "Bread" from Running Low lands in Pantry.
- Adding the same name again increases quantity and announces it.
- The add input has an accessible name (`getByRole('textbox', { name: /item name/i })`).
- Undo after clear list restores the soft-deleted items (existing `pendingUndo` path).

**Verify:**
```bash
npx vitest run src/__tests__/pages/ShoppingList.test.tsx src/__tests__/pages/Alerts.test.tsx
npm run typecheck
npm run lint
```

---

### Task 3: Meal Planner characterization tests

**Files:**
- Create: `src/__tests__/pages/MealPlanner.test.tsx`
- Modify: `src/pages/MealPlanner.tsx` only if a test cannot target existing accessible names (smallest a11y hook, no visual redesign)

**Do not edit:** shopping list files, receipt files, `App.tsx`, CSS hotspots.

**Cover:**
- Renders the weekly planner heading/slots from `getOrCreateWeekPlan`.
- Opening a slot shows the recipe picker dialog and moves focus (existing `useModalFocus`).
- Escape / Close returns focus and hides the picker.
- `Add missing to list` (or the current button label) calls `addMissingToShoppingList` when missing count > 0.

Mock `mealPlanService` and `InventoryContext` like other page tests. Use `useModalFocus` tests as the focus contract.

**Verify:**
```bash
npx vitest run src/__tests__/pages/MealPlanner.test.tsx src/__tests__/hooks/useModalFocus.test.tsx
npm run typecheck
npm run lint
```
