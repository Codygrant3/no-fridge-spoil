# IPIE Cycle 12 Implementation Plan

> Extend draft `#17`. Do not open a second meal-plan branch.

**Goal:** Meal-plan missing-ingredient shopping rows use `shoppingCategory`.

## Task: Meal-plan shopping category

**Branch:** `cursor/meal-plan-week-start-3203` (existing `#17`)

**Files:**
- Modify: `src/services/mealPlanService.ts`
- Modify: `src/__tests__/services/mealPlanService.test.ts`
- Modify: `src/services/shoppingActionService.ts` (identical `export` to `#8`)

**Behavior:**
- `addMissingToShoppingList` sets `category: shoppingCategory(ingredient)`.
- Export `shoppingCategory` with the same one-line change `#8` already has.

**Tests:**
- Adding eggs records `category: 'dairy'`.

**Verify:**
```bash
npx vitest run src/__tests__/services/mealPlanService.test.ts
npm run typecheck
npm run lint
```

After `#17` lands, skip `docs/superpowers/patches/2026-08-22-phase-c-meal-plan-category.patch`.
