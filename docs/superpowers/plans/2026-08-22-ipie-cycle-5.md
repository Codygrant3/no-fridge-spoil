# IPIE Cycle 5 Implementation Plan

> **For agentic workers:** One concern per agent. Branch from `main` (`0c8fbf4`). Do not edit files listed on open draft PRs #3–#14.

**Goal:** Stop writing `YYYY-MM-DD` grocery dates with UTC `Date` / `toISOString()` on three free surfaces.

## Global Constraints

- Branch name must match `cursor/<descriptive-name>-3203` (lowercase).
- Never reset or delete untracked files.
- No secrets in `VITE_*`.
- Do not touch `Scan.tsx`, `ReviewItems.tsx`, `Profile.tsx`, `index.css`, `App.tsx`, receipt OCR/resolver/recovery, `api/*`, shopping list files, Alerts, CookMode, EatThisTonight, Recipes tests, or IPIE docs.
- Reuse `parseDate`, `formatDate`, `daysUntilExpiration`, and `calculateExpirationFromShelfLife` from `src/utils/dateUtils.ts`. Do not add a second date helper.
- Focused vitest + `npm run typecheck` + `npm run lint`. Do not claim skipped release gates passed.
- Push the branch. Do not open or merge a pull request (coordinator opens the draft).

---

### Task 1: Inventory local calendar

**Branch:** `cursor/inventory-local-calendar-3203`

**Files:**
- Modify: `src/pages/Inventory.tsx`
- Modify: `src/__tests__/pages/Inventory.test.tsx`

**Behavior:**
- `getDaysUntil` and urgency sort must use `daysUntilExpiration` / `parseDate`, not `new Date('YYYY-MM-DD')`.
- `markAsOpened` writes `formatDate(new Date())`, not `toISOString().split('T')[0]`.
- Freeze (+30 days) writes `formatDate` of the local date, not UTC ISO.
- Search input gets an accessible name (`Search groceries` or equivalent). Keep Search and Filter both opening the existing panel.

**Tests (keep the existing batching test):**
- Attention filter hides an item more than 7 local days out and shows one due today / within 7 days.
- Freeze on a fridge item due in 2–3 days calls `updateItem` with `storageLocation: 'freezer'` and `expirationDate` equal to `formatDate(today + 30)`.
- Opened writes today's `formatDate`.
- Search field is findable with `getByRole('textbox', { name: /search groceries/i })`.

**Verify:**
```bash
npx vitest run src/__tests__/pages/Inventory.test.tsx src/__tests__/utils/dateUtils.test.ts
npm run typecheck
npm run lint
```

---

### Task 2: Meal-plan local week start

**Branch:** `cursor/meal-plan-week-start-3203`

**Files:**
- Modify: `src/services/mealPlanService.ts`
- Create: `src/__tests__/services/mealPlanService.test.ts`

**Do not edit:** `src/pages/MealPlanner.tsx`, `src/__tests__/pages/MealPlanner.test.tsx`, shopping files.

**Behavior:**
- `getCurrentWeekStart` returns the local Monday as `YYYY-MM-DD` via `formatDate`.
- Optional `now = new Date()` argument is allowed so tests can pass a local Sunday evening and a Monday morning.
- Do not change shopping-list category (`other`) this cycle — that file is on #8.
- Keep household / `localMutationFields` behavior.

**Tests:**
- Sunday local date → that week's Monday (`formatDate`), not `toISOString().split('T')[0]` of the shifted Date.
- Monday local date → itself.
- `getAllIngredients` / `addMissingToShoppingList` still skip names already on the list (one Dexie test is enough).

**Verify:**
```bash
npx vitest run src/__tests__/services/mealPlanService.test.ts
npm run typecheck
npm run lint
```

---

### Task 3: Sealed shelf-life local dates

**Branch:** `cursor/sealed-shelf-life-dates-3203`

**Files:**
- Modify: `src/services/sealedShelfLifeService.ts`
- Create: `src/__tests__/services/sealedShelfLifeService.test.ts`

**Do not edit:** `Scan.tsx`, `ReviewItems.tsx`, or any other consumer.

**Behavior:**
- `estimateExpirationDate(days)` must return `calculateExpirationFromShelfLife(days)`.
- Do not widen or reorder fuzzy matching.

**Tests:**
- `estimateExpirationDate(n)` equals `calculateExpirationFromShelfLife(n)`.
- `getShelfLifeDefaults('sour cream')` is the sour-cream rule, not generic cream.
- `getShelfLifeDefaults('heavy cream')` stays heavy/whipping cream.
- Unknown name returns `null`.

**Verify:**
```bash
npx vitest run src/__tests__/services/sealedShelfLifeService.test.ts src/__tests__/utils/dateUtils.test.ts
npm run typecheck
npm run lint
```
