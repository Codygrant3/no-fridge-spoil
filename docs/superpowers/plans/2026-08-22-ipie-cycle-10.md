# IPIE Cycle 10 Implementation Plan

> Branch from `main` (`0c8fbf4`). Do not edit files on draft PRs #3–#24.

**Goal:** Hosted CI can run the cloud foundation + cloud E2E gate against a disposable local Supabase stack.

## Task: Disposable cloud CI job

**Branch:** `cursor/cloud-ci-job-3203`

**Files:**
- Modify: `.github/workflows/ci.yml`, `package.json`, `supabase/config.toml`
- Create: `src/utils/localSupabaseEnv.ts`, `src/__tests__/utils/localSupabaseEnv.test.ts`, `scripts/write-local-supabase-env.ts`, `supabase/seed.sql`

**Behavior:**
- Add a `cloud` GitHub Actions job that starts local Supabase with Docker, writes a generated `.env.local`, then runs `verify:cloud` and `test:e2e:cloud`.
- Never put `SUPABASE_SECRET_KEY` in a `VITE_*` variable.
- Refuse to overwrite a hand-written `.env.local` unless `--force`.
- Allow local Vite ports in Auth redirect URLs so cloud E2E signup on `:5175` is valid.
- Do not claim `verify:release:cloud` passed unless that full command ran.

**Tests:**
- Status-env parser maps publishable/secret keys and keeps the secret off `VITE_*`.
- Overwrite guard refuses a hand-written env file.

**Verify:**
```bash
npx vitest run src/__tests__/utils/localSupabaseEnv.test.ts
npm run typecheck
npm run lint
```

Hosted `cloud` job is the live gate. This environment cannot start Docker/Supabase.
