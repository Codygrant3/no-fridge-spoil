# IPIE Cycle 11 Implementation Plan

> Extend draft `#14`. Do not open a second Alerts branch.

**Goal:** Alerts freeze writes a local `YYYY-MM-DD`, not a UTC slice.

## Task: Alerts freeze local date

**Branch:** `cursor/alerts-shopping-calendar-tests-9571` (existing `#14`)

**Files:**
- Modify: `src/pages/Alerts.tsx`
- Modify: `src/__tests__/pages/Alerts.test.tsx`

**Behavior:**
- `freezeItem` stores `formatDate(frozenUntil)` after `setDate(+30)`.
- Keep shopping/calendar tests and mocked `shoppingActionService`.

**Tests:**
- Click Freeze on the expiring fridge item.
- `updateItem` receives `storageLocation: 'freezer'` and `expirationDate: formatDate(local + 30)`.
- Status text still says the item moved to the freezer for 30 days.

**Verify:**
```bash
npx vitest run src/__tests__/pages/Alerts.test.tsx
npm run typecheck
npm run lint
```

After `#14` lands, skip `docs/superpowers/patches/2026-08-22-phase-c-alerts-freeze.patch`.
