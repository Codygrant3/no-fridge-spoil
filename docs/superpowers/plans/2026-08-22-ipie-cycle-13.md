# IPIE Cycle 13 Implementation Plan

> Extend drafts `#5` and `#3`. Do not open new Phase C branches.

**Goal:** Scan cancel passes `AbortSignal`. Recovery resumes a stored receipt `jobId`.

## Task: Scan cancel + resume API

**Branch:** `cursor/normalize-ocr-diagnostics-14d7` (existing `#5`)

**Files:**
- Modify: `src/pages/Scan.tsx`
- Modify: `src/__tests__/pages/Scan.receipt.test.tsx`
- Modify: `src/services/imageCompressionService.ts` (same signal API as `#23`)
- Modify: `src/services/receiptOCRService.ts`
- Modify: `src/__tests__/services/receiptOCRService.test.ts`

## Task: Recovery jobId persist

**Branch:** `cursor/receipt-recovery-online-9e8a` (existing `#3`)

**Files:**
- Modify: `src/services/receiptRecoveryService.ts`
- Modify: `src/__tests__/services/receiptRecoveryService.test.ts`
- Modify: `src/services/receiptOCRService.ts` (`jobId` / `resumeJobId` types only)

**Behavior:**
- Cancel scan aborts compression and does not queue.
- Valid `resumeJobId` skips `POST /api/receipt-jobs`.
- Recovery still passes stored `cloudOcrConsent` and never sets consent true.

After these heads land, skip the remaining Phase C patches.
