# IPIE Cycle 7 Implementation Plan

> Branch from `main` (`0c8fbf4`). Do not edit files on draft PRs #3–#19.

**Goal:** Characterize kitchen setup and make barcode manual entry findable.

## Global Constraints

- Branch name `cursor/<descriptive-name>-3203` (lowercase).
- Never reset or delete untracked files. No secrets in `VITE_*`.
- Do not touch Profile, ProfileSwitcher, Inventory, Scan, ReviewItems, shopping, meal-plan, sealed shelf-life, Alerts, receipt OCR/resolver/recovery, `api/*`, `index.css`, or IPIE docs.
- Focused vitest + `npm run typecheck` + `npm run lint`. Do not claim skipped gates passed.
- Push the branch. Do not open or merge a PR.

---

### Task 1: Onboarding carousel tests

**Branch:** `cursor/onboarding-carousel-tests-3203`

**Files:**
- Create: `src/__tests__/components/OnboardingCarousel.test.tsx`
- Modify `src/components/OnboardingCarousel.tsx` only if a test cannot target an existing name (smallest hook).

**Cover:**
- Renders “Get to your first saved item” when not dismissed.
- Hide setup checklist dismisses and writes the dismissed localStorage key.
- `itemCount === 0` shows Scan; click calls `onStartClick`. Enable reminders is disabled.
- Seed `db.settings` user row. Click `3d` sets `expirationWarningDays` to 3.
- Denied notification permission shows the existing alert copy. Never auto-grant.

**Verify:**
```bash
npx vitest run src/__tests__/components/OnboardingCarousel.test.tsx
npm run typecheck
npm run lint
```

---

### Task 2: Barcode scanner manual name

**Branch:** `cursor/barcode-manual-name-3203`

**Files:**
- Modify: `src/components/BarcodeScanner.tsx`
- Create: `src/__tests__/components/BarcodeScanner.test.tsx`

**Behavior:**
- Manual barcode input accessible name `Barcode number` (label or aria-label).
- Do not start a real camera in tests. Mock `html5-qrcode` and `lookupBarcode`.
- If `getUserMedia` is missing, the existing HTTPS error copy must remain.

**Tests:**
- Close scanner calls `onClose` (`Close scanner`).
- Manual → type digits → Look Up Product calls `lookupBarcode` then `onProductFound`.
- Manual field: `getByRole('textbox', { name: /barcode number/i })`.
- Missing `getUserMedia` shows the HTTPS / photo-capture message.

**Verify:**
```bash
npx vitest run src/__tests__/components/BarcodeScanner.test.tsx src/__tests__/services/barcodeService.test.ts
npm run typecheck
npm run lint
```
