# IPIE Cycle 6 Implementation Plan

> Branch from `main` (`0c8fbf4`). Do not edit files on draft PRs #3–#17.

**Goal:** Characterize the Profile hotspot without splitting it, and fix ProfileSwitcher create-field naming.

## Global Constraints

- Branch name `cursor/<descriptive-name>-3203` (lowercase).
- Never reset or delete untracked files. No secrets in `VITE_*`.
- Do not touch Scan, ReviewItems, Inventory, Alerts, shopping, meal-plan service, sealed shelf-life, receipt OCR/resolver/recovery, `api/*`, `index.css`, or IPIE docs.
- Consent stays explicit. Never set cloud OCR / receipt upload consent to true in tests or code.
- Focused vitest + `npm run typecheck` + `npm run lint`. Do not claim skipped gates passed.
- Push the branch. Do not open or merge a PR.

---

### Task 1: Profile characterization

**Branch:** `cursor/profile-characterization-3203`

**Files:**
- Create: `src/__tests__/pages/Profile.test.tsx`
- Modify `src/pages/Profile.tsx` only if a test cannot target an existing accessible name (smallest hook, no layout/CSS split).

**Cover (local-first / unsigned-in):**
- Renders `Profile` and local sections: Device storage, Receipt intelligence, Device backup, privacy.
- Health-check button is present and, when clicked, calls `checkReceiptOcrHealth` (mock `receiptOCRService`).
- Device backup download control is present.
- Cloud account / Household sections stay hidden when auth is unconfigured.
- Do not change privacy defaults or auto-enable consent.

**Verify:**
```bash
npx vitest run src/__tests__/pages/Profile.test.tsx src/__tests__/components/AccountAccess.test.tsx
npm run typecheck
npm run lint
```

---

### Task 2: ProfileSwitcher name + tests

**Branch:** `cursor/profile-switcher-a11y-3203`

**Files:**
- Modify: `src/components/ProfileSwitcher.tsx`
- Create: `src/__tests__/components/ProfileSwitcher.test.tsx`

**Behavior:**
- Create-profile name field gets an accessible name (`Profile name` via label or `aria-label`).
- Trigger already shows Household / profile name; add `aria-haspopup="dialog"` or `aria-label="Switch profile"` only if needed for a stable query.
- Mock `useProfile` from `ProfileContext`.

**Tests:**
- Open switcher, switch to a named profile.
- Open create, type a name, Create calls `createProfile` then `switchProfile`.
- Delete confirm / cancel stay labeled (`Delete {name}`, `Cancel delete`).

**Verify:**
```bash
npx vitest run src/__tests__/components/ProfileSwitcher.test.tsx
npm run typecheck
npm run lint
```
